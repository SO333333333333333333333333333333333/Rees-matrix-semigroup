Reesの正規行列半群を計算するプログラムです。
デフォルトではeが単位元、a^2=eで定義される位数2の群G={e, a}と、
G上の3x2行列 P=
[
  [e, 0],
  [0, e],
  [a, a]
]
について p(l, m)をPの(l, m)成分とすると
(k, x, l)(m, y, n) = (k, xp(l, m)y, n) (ただしp(l, m)=0のときは0)
とS={0, 1}xGx{0, 1, 2}∪{0}上に演算を定義した完全0-単純半群を計算します。
